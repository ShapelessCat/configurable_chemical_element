[workspace]
members = [
    "macro",
    "api",
]

resolver = "2"

[profile.release]
debug = false
strip = true
lto = "fat"

[profile.cpu-profiling]
inherits = "release"
debug = true
strip = false
lto = "fat"
